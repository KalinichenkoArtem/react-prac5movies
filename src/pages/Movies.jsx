import { Form } from './Movies.styled';

export const Movies = () => {
  return (
    <main>
      <Form>
        <input type="text" />
        <button>Search</button>
      </Form>
    </main>
  );
};

export default Movies;
